module.exports.items = ['item1', 'item2'];
const person = {
  name: 'bob',
};

module.exports.singlePerson = person;
// we are able to write this because module.exports is an object and we are setting a property to that object.

// we are learning modules
